package mods;

/**
 * Used to determine the path to a specific file.
 * Each type corresponds to one file.
 */
public enum FileType {
    OUTPUT,
    USER_ERRORS,
    SYSTEM_ERRORS,
    JSON,
    REFERENCE;
}
